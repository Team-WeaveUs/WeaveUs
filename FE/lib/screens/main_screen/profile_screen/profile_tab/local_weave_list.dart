import 'package:flutter/material.dart';

class LocalWeaveList extends StatelessWidget {
  const LocalWeaveList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("비밀 위브 리스트 들어갈 곳"),
    );
  }
}